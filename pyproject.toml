[project]
name = "vibe-office"
version = "0.1.0"
description = "Chat-based AI agent for Excel and Word file operations"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "openai>=1.50.0",
    "openpyxl>=3.1.0",
    "python-docx>=1.1.0",
]

[tool.uv]
package = false
